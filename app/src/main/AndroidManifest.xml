<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test.nuvoco3">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splashscreen.SplashScreenActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <!-- <activity -->
        <!-- android:name="com.example.test.nuvoco3.MainActivity" -->
        <!-- android:label="@string/app_name" > -->
        <!-- </activity> -->

        <activity android:name=".InsertCustomerActivity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".viewcustomerdata.ViewCustomerActivity"
            android:label="@string/title_activity_view_customer"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- <activity android:name=".splashscreen.SplashScreenActivity"></activity> -->
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme" />
        <activity android:name=".InsertCustomerContactActivity"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>